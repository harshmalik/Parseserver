#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../parse-server/bin/parse-server" "$@"
  ret=$?
else 
  node  "$basedir/../parse-server/bin/parse-server" "$@"
  ret=$?
fi
exit $ret
